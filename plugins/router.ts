export default defineNuxtPlugin((nuxtApp) => {
    // console.log('nuxtApp :>> ', nuxtApp);
    // const router = useRouter()
    // console.log('object router :>> ', router);

    // nuxtApp.hook('pages:extend', pages => {
    //     console.log(`发现了${pages.length}个页面`)
    //   })
})
